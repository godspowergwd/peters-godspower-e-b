# Test Environment Variables
# DO NOT USE PRODUCTION SECRETS HERE

# Server Configuration
PORT=5001 # Use a different port for testing if server is started
NODE_ENV=test

# Database Configuration (PostgreSQL example - not used by in-memory tests yet)
DB_USER=test_db_user
DB_PASSWORD=test_db_password
DB_HOST=localhost
DB_PORT=5433 # Different port if needed
DB_NAME=marketing_app_db_test

# JWT Configuration
JWT_SECRET=-----BEGIN RSA PRIVATE KEY-----\nSUPER_SECRET_TEST_KEY_REPLACE_WITH_REAL_ONE_FOR_PROD_MIN_32_CHARS_LONG\n-----END RSA PRIVATE KEY-----
JWT_EXPIRES_IN=15m # Shorter expiry for tests if needed
JWT_ADMIN_EXPIRES_IN=10m

# Email Service (Mocked, so API key not critical for tests unless testing service directly)
SENDGRID_API_KEY=test_sendgrid_api_key
FROM_EMAIL=test-noreply@example.com

# Stripe Configuration (Use Stripe's test mode keys)
STRIPE_SECRET_KEY=sk_test_yourteststripesecretkey
STRIPE_PUBLISHABLE_KEY=pk_test_yourteststripepublishablekey
STRIPE_WEBHOOK_SECRET=whsec_test_yourteststripewebhooksecret
STRIPE_SUCCESS_URL=http://localhost:3001/test-payment-success?session_id={CHECKOUT_SESSION_ID}
STRIPE_CANCEL_URL=http://localhost:3001/test-payment-cancelled

# Client URL (if needed by backend during tests)
CLIENT_URL=http://localhost:3001

# Admin User Seeding (Development/Test Only)
ADMIN_EMAIL=admintest@example.com
ADMIN_PASSWORD=AdminTestPassword123!
